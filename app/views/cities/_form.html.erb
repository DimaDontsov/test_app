<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div class="alert alert-danger" id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Название"%> <br>
    <%= form.text_field :name, id: :city_name %>
  </div>

  <div class="field">
    <%= form.label :region_id, "Регион, в котором расположен город" %> <br>
    <%= form.select :region_id, Region.all.collect{|c| [c.name, c.id]} %>
  </div>

  <div class="actions">
    <br>
    <%= form.submit "Сохранить" %>
  </div>
<% end %>
